{
  "name": "login-usuarios-cooperativa",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "sass --load-path=node_modules --watch src/scss/main.scss:css/main.css",
    "build": "sass --load-path=node_modules src/scss/main.scss:css/main.css --no-source-map --style=compressed --quiet-deps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/QuantixGroup/login-usuarios-cooperativa.git"
  },
  "author": "Quantix",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/QuantixGroup/login-usuarios-cooperativa/issues"
  },
  "homepage": "https://github.com/QuantixGroup/login-usuarios-cooperativa#readme",
  "description": "",
  "dependencies": {
    "bootstrap": "^5.3.7",
    "sass": "^1.93.0"
  }
}
